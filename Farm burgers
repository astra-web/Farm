local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer

local farmEnabled = false
local ghostFlyEnabled = false
local normalFlyEnabled = false

local ghostLoop
local flyLoop

local bodyClone
local startPos

local flySpeed = 50
local maxSpeed = 1000

local ctrl = {
	f = 0,
	b = 0,
	l = 0,
	r = 0
}

local lastctrl = {
	f = 0,
	b = 0,
	l = 0,
	r = 0
}

local originalTransparencies = {}
local originalCollisions = {}

local targetNames = {
	"FireCrateCurrency",
	"Burger"
}

--==================================================
-- GUI
--==================================================

local gui = Instance.new("ScreenGui")
gui.Name = "FarmUI"
gui.ResetOnSpawn = false
gui.Parent = player:WaitForChild("PlayerGui")

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 290, 0, 285)
frame.Position = UDim2.new(0.5, -145, 0.5, -142)
frame.BackgroundColor3 = Color3.fromRGB(25,25,25)
frame.BorderSizePixel = 0
frame.Active = true
frame.Parent = gui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0,12)
corner.Parent = frame

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1,0,0,40)
title.BackgroundTransparency = 1
title.Text = "Farm 💎 Crystal / 🍔 Burger"
title.TextColor3 = Color3.new(1,1,1)
title.TextSize = 18
title.Font = Enum.Font.GothamBold
title.Parent = frame

local function createButton(text,y)

	local button = Instance.new("TextButton")

	button.Size = UDim2.new(0.85,0,0,38)
	button.Position = UDim2.new(0.075,0,0,y)

	button.BackgroundColor3 =
		Color3.fromRGB(45,45,45)

	button.Text = text
	button.TextColor3 = Color3.new(1,1,1)
	button.TextSize = 15
	button.Font = Enum.Font.GothamBold

	button.Parent = frame

	local c = Instance.new("UICorner")
	c.CornerRadius = UDim.new(0,10)
	c.Parent = button

	return button
end

local farmButton =
	createButton("Farm: Выключено ❎",45)

local ghostButton =
	createButton("👻 Ghost Fly: Выключено ❎",88)

local flyButton =
	createButton("✈️ Fly: Выключено ❎",131)

local speedBox = Instance.new("TextBox")

speedBox.Size = UDim2.new(0.85,0,0,35)
speedBox.Position = UDim2.new(0.075,0,0,176)

speedBox.BackgroundColor3 =
	Color3.fromRGB(45,45,45)

speedBox.Text = "50"
speedBox.PlaceholderText = "Fly Speed"

speedBox.TextColor3 =
	Color3.new(1,1,1)

speedBox.TextSize = 15
speedBox.Font = Enum.Font.GothamBold

speedBox.ClearTextOnFocus = false

speedBox.Parent = frame

local speedCorner = Instance.new("UICorner")
speedCorner.CornerRadius = UDim.new(0,10)
speedCorner.Parent = speedBox

local minimizeButton =
	createButton("Свернуть ➖",224)

--==================================================
-- MINI UI
--==================================================

local miniFrame = Instance.new("TextButton")

miniFrame.Size =
	UDim2.new(0,120,0,38)

miniFrame.Position =
	frame.Position

miniFrame.BackgroundColor3 =
	Color3.fromRGB(25,25,25)

miniFrame.Text = "☰ Farm"

miniFrame.TextColor3 =
	Color3.new(1,1,1)

miniFrame.TextSize = 15
miniFrame.Font = Enum.Font.GothamBold

miniFrame.Visible = false
miniFrame.Active = true
miniFrame.Parent = gui

local miniCorner = Instance.new("UICorner")
miniCorner.CornerRadius = UDim.new(0,12)
miniCorner.Parent = miniFrame

--==================================================
-- FARM
--==================================================

farmButton.MouseButton1Click:Connect(function()

	farmEnabled = not farmEnabled

	if farmEnabled then

		farmButton.Text =
			"Farm: Включено ✅"

	else

		farmButton.Text =
			"Farm: Выключено ❎"
	end
end)

--==================================================
-- GHOST MODE
--==================================================

local function setGhostMode(state)

	local character =
		player.Character

	if not character then
		return
	end

	if state then

		for _,part in ipairs(
			character:GetDescendants()
		) do

			if part:IsA("BasePart")
				or part:IsA("Decal") then

				if part.Name ~=
					"HumanoidRootPart" then

					if originalTransparencies[part]
						== nil then

						originalTransparencies[part] =
							part.Transparency
					end

					part.Transparency = 0.5
				end
			end
		end

	else

		for part,transparency in pairs(
			originalTransparencies
		) do

			if part and part.Parent then
				part.Transparency =
					transparency
			end
		end

		table.clear(originalTransparencies)
	end
end

--==================================================
-- GHOST FLY
--==================================================

local function startGhostFly()

	local character =
		player.Character

	if not character then
		return
	end

	local humanoid =
		character:FindFirstChildOfClass(
			"Humanoid"
		)

	local head =
		character:FindFirstChild("Head")

	local hrp =
		character:FindFirstChild(
			"HumanoidRootPart"
		)

	if not humanoid or not head or not hrp then
		return
	end

	startPos = hrp.CFrame

	if bodyClone then
		bodyClone:Destroy()
	end

	character.Archivable = true

	bodyClone = character:Clone()
	bodyClone.Name = "GhostFlyVessel"

	for _,object in ipairs(
		bodyClone:GetDescendants()
	) do

		if object:IsA("BasePart") then

			object.Anchored = true
			object.CanCollide = false

		elseif object:IsA("Script")
			or object:IsA("LocalScript") then

			object:Destroy()
		end
	end

	bodyClone.Parent = workspace

	bodyClone:PivotTo(startPos)

	humanoid.PlatformStand = true
	head.Anchored = true

	setGhostMode(true)

	if ghostLoop then
		ghostLoop:Disconnect()
	end

	ghostLoop =
		RunService.Heartbeat:Connect(
		function(deltaTime)

			if not ghostFlyEnabled
				or not character.Parent then
				return
			end

			local moveDirection =
				humanoid.MoveDirection
				* (flySpeed * deltaTime)

			local headCFrame =
				head.CFrame

			local camera =
				workspace.CurrentCamera

			local cameraCFrame =
				camera.CFrame

			local cameraOffset =
				headCFrame:ToObjectSpace(
					cameraCFrame
				).Position

			cameraCFrame =
				cameraCFrame
				* CFrame.new(
					-cameraOffset.X,
					-cameraOffset.Y,
					-cameraOffset.Z + 1
				)

			local cameraPosition =
				cameraCFrame.Position

			local headPosition =
				headCFrame.Position

			local objectSpaceVelocity =
				CFrame.new(
					cameraPosition,
					Vector3.new(
						headPosition.X,
						cameraPosition.Y,
						headPosition.Z
					)
				):VectorToObjectSpace(
					moveDirection
				)

			head.CFrame =
				CFrame.new(headPosition)
				* (cameraCFrame-cameraPosition)
				* CFrame.new(
					objectSpaceVelocity
				)
		end
	)
end

local function stopGhostFly()

	if ghostLoop then
		ghostLoop:Disconnect()
		ghostLoop = nil
	end

	local character =
		player.Character

	if character then

		local humanoid =
			character:FindFirstChildOfClass(
				"Humanoid"
			)

		local head =
			character:FindFirstChild("Head")

		if humanoid then
			humanoid.PlatformStand = false
		end

		if head then
			head.Anchored = false
		end
	end

	setGhostMode(false)

	if bodyClone then
		bodyClone:Destroy()
		bodyClone = nil
	end
end

--==================================================
-- NORMAL FLY TRANSPARENCY
--==================================================

local function setFlyTransparency(state)

	local character =
		player.Character

	if not character then
		return
	end

	for _,object in ipairs(
		character:GetDescendants()
	) do

		if object:IsA("BasePart")
			or object:IsA("Decal") then

			if state then

				if originalTransparencies[object]
					== nil then

					originalTransparencies[object] =
						object.Transparency
				end

				if object.Name ~=
					"HumanoidRootPart" then

					object.Transparency = 0.5
				end

			else

				if originalTransparencies[object]
					~= nil then

					object.Transparency =
						originalTransparencies[object]
				end
			end
		end
	end

	if not state then
		table.clear(originalTransparencies)
	end
end

--==================================================
-- NORMAL FLY NOCLIP
--==================================================

local function setFlyCollision(state)

	local character =
		player.Character

	if not character then
		return
	end

	for _,part in ipairs(
		character:GetDescendants()
	) do

		if part:IsA("BasePart") then

			if state then

				if originalCollisions[part]
					== nil then

					originalCollisions[part] =
						part.CanCollide
				end

				part.CanCollide = false

			else

				if originalCollisions[part]
					~= nil then

					part.CanCollide =
						originalCollisions[part]
				end
			end
		end
	end

	if not state then
		table.clear(originalCollisions)
	end
end

--==================================================
-- ORIGINAL STYLE FLY
--==================================================

local function startNormalFly()

	local character =
		player.Character

	if not character then
		return
	end

	local humanoid =
		character:FindFirstChildOfClass(
			"Humanoid"
		)

	local torso =
		character:FindFirstChild(
			"HumanoidRootPart"
		)

	if not humanoid or not torso then
		return
	end

	-- сброс управления

	ctrl = {
		f = 0,
		b = 0,
		l = 0,
		r = 0
	}

	lastctrl = {
		f = 0,
		b = 0,
		l = 0,
		r = 0
	}

	flySpeed =
		tonumber(speedBox.Text)
		or 50

	-- BodyGyro

	local bg =
		Instance.new("BodyGyro")

	bg.Name = "AstraFlyGyro"

	bg.P = 9e4

	bg.MaxTorque =
		Vector3.new(
			9e9,
			9e9,
			9e9
		)

	bg.CFrame =
		torso.CFrame

	bg.Parent = torso

	-- BodyVelocity

	local bv =
		Instance.new("BodyVelocity")

	bv.Name = "AstraFlyVelocity"

	bv.Velocity =
		Vector3.new(0,0.1,0)

	bv.MaxForce =
		Vector3.new(
			9e9,
			9e9,
			9e9
		)

	bv.Parent = torso

	humanoid.PlatformStand = true

	setFlyCollision(true)
	setFlyTransparency(true)

	if flyLoop then
		flyLoop:Disconnect()
	end

	flyLoop =
		RunService.RenderStepped:Connect(
		function()

			if not normalFlyEnabled
				or not character.Parent then

				return
			end

			local camera =
				workspace.CurrentCamera

			-- ускорение

			if ctrl.l + ctrl.r ~= 0
				or ctrl.f + ctrl.b ~= 0 then

				flySpeed =
					flySpeed
					+ 0.5
					+ (flySpeed/maxSpeed)

				if flySpeed > maxSpeed then
					flySpeed = maxSpeed
				end

			else

				if flySpeed ~= 0 then

					flySpeed =
						flySpeed - 1

					if flySpeed < 0 then
						flySpeed = 0
					end
				end
			end

			-- движение

			if ctrl.l + ctrl.r ~= 0
				or ctrl.f + ctrl.b ~= 0 then

				bv.Velocity =
					(
						camera.CFrame.LookVector
						* (ctrl.f + ctrl.b)
					)
					+
					(
						(
							camera.CFrame
							* CFrame.new(
								ctrl.l + ctrl.r,
								(ctrl.f + ctrl.b)*0.2,
								0
							)
						).Position
						- camera.CFrame.Position
					)

					bv.Velocity =
						bv.Velocity * flySpeed

				lastctrl = {
					f = ctrl.f,
					b = ctrl.b,
					l = ctrl.l,
					r = ctrl.r
				}

			elseif
				ctrl.l + ctrl.r == 0
				and ctrl.f + ctrl.b == 0
				and flySpeed ~= 0 then

				bv.Velocity =
					(
						camera.CFrame.LookVector
						* (lastctrl.f + lastctrl.b)
					)
					+
					(
						(
							camera.CFrame
							* CFrame.new(
								lastctrl.l + lastctrl.r,
								(lastctrl.f + lastctrl.b)*0.2,
								0
							)
						).Position
						- camera.CFrame.Position
					)

					bv.Velocity =
						bv.Velocity * flySpeed

			else

				bv.Velocity =
					Vector3.new(
						0,
						0.1,
						0
					)
			end

			bg.CFrame =
				camera.CFrame
				* CFrame.Angles(
					-math.rad(
						(ctrl.f + ctrl.b)
						* 50
						* flySpeed
						/ maxSpeed
					),
					0,
					0
				)

			-- noclip

			for _,part in ipairs(
				character:GetDescendants()
			) do

				if part:IsA("BasePart") then
					part.CanCollide = false
				end
			end
		end
	)
end

local function stopNormalFly()

	if flyLoop then
		flyLoop:Disconnect()
		flyLoop = nil
	end

	local character =
		player.Character

	if character then

		local humanoid =
			character:FindFirstChildOfClass(
				"Humanoid"
			)

		local torso =
			character:FindFirstChild(
				"HumanoidRootPart"
			)

		if humanoid then
			humanoid.PlatformStand = false
		end

		if torso then

			local bg =
				torso:FindFirstChild(
					"AstraFlyGyro"
				)

			local bv =
				torso:FindFirstChild(
					"AstraFlyVelocity"
				)

			if bg then
				bg:Destroy()
			end

			if bv then
				bv:Destroy()
			end
		end
	end

	setFlyCollision(false)
	setFlyTransparency(false)

	flySpeed =
		tonumber(speedBox.Text)
		or 50
end

--==================================================
-- KEYBOARD CONTROL FOR FLY
--==================================================

UserInputService.InputBegan:Connect(
	function(input,gameProcessed)

		if gameProcessed then
			return
		end

		local key =
			input.KeyCode

		if key == Enum.KeyCode.W then
			ctrl.f = 1

		elseif key == Enum.KeyCode.S then
			ctrl.b = -1

		elseif key == Enum.KeyCode.A then
			ctrl.l = -1

		elseif key == Enum.KeyCode.D then
			ctrl.r = 1
		end
	end
)

UserInputService.InputEnded:Connect(
	function(input)

		local key =
			input.KeyCode

		if key == Enum.KeyCode.W then
			ctrl.f = 0

		elseif key == Enum.KeyCode.S then
			ctrl.b = 0

		elseif key == Enum.KeyCode.A then
			ctrl.l = 0

		elseif key == Enum.KeyCode.D then
			ctrl.r = 0
		end
	end
)

--==================================================
-- GHOST BUTTON
--==================================================

ghostButton.MouseButton1Click:Connect(function()

	if normalFlyEnabled then

		normalFlyEnabled = false

		stopNormalFly()

		flyButton.Text =
			"✈️ Fly: Выключено ❎"
	end

	ghostFlyEnabled =
		not ghostFlyEnabled

	if ghostFlyEnabled then

		startGhostFly()

		ghostButton.Text =
			"👻 Ghost Fly: Включено ✅"

	else

		stopGhostFly()

		ghostButton.Text =
			"👻 Ghost Fly: Выключено ❎"
	end
end)

--==================================================
-- FLY BUTTON
--==================================================

flyButton.MouseButton1Click:Connect(function()

	if ghostFlyEnabled then

		ghostFlyEnabled = false

		stopGhostFly()

		ghostButton.Text =
			"👻 Ghost Fly: Выключено ❎"
	end

	normalFlyEnabled =
		not normalFlyEnabled

	if normalFlyEnabled then

		flySpeed =
			tonumber(speedBox.Text)
			or 50

		startNormalFly()

		flyButton.Text =
			"✈️ Fly: Включено ✅"

	else

		stopNormalFly()

		flyButton.Text =
			"✈️ Fly: Выключено ❎"
	end
end)

--==================================================
-- SPEED
--==================================================

speedBox.FocusLost:Connect(function()

	local value =
		tonumber(speedBox.Text)

	if value and value > 0 then

		flySpeed = value

	else

		speedBox.Text =
			tostring(flySpeed)
	end
end)

--==================================================
-- DRAG MAIN UI
--==================================================

local dragging = false
local dragStart
local startPosUI

frame.InputBegan:Connect(function(input)

	if input.UserInputType ==
		Enum.UserInputType.MouseButton1
		or input.UserInputType ==
		Enum.UserInputType.Touch then

		dragging = true

		dragStart =
			input.Position

		startPosUI =
			frame.Position

		input.Changed:Connect(function()

			if input.UserInputState ==
				Enum.UserInputState.End then

				dragging = false
			end
		end)
	end
end)

UserInputService.InputChanged:Connect(function(input)

	if dragging and (
		input.UserInputType ==
		Enum.UserInputType.MouseMovement
		or input.UserInputType ==
		Enum.UserInputType.Touch
	) then

		local delta =
			input.Position - dragStart

		frame.Position =
			UDim2.new(
				startPosUI.X.Scale,
				startPosUI.X.Offset + delta.X,

				startPosUI.Y.Scale,
				startPosUI.Y.Offset + delta.Y
			)
	end
end)

--==================================================
-- MINIMIZE
--==================================================

local minimized = false

minimizeButton.MouseButton1Click:Connect(function()

	minimized = true

	miniFrame.Position =
		frame.Position

	frame.Visible = false
	miniFrame.Visible = true
end)

miniFrame.MouseButton1Click:Connect(function()

	minimized = false

	frame.Position =
		miniFrame.Position

	frame.Visible = true
	miniFrame.Visible = false
end)

--==================================================
-- DRAG MINI UI
--==================================================

local miniDragging = false
local miniDragStart
local miniStartPos

miniFrame.InputBegan:Connect(function(input)

	if input.UserInputType ==
		Enum.UserInputType.MouseButton1
		or input.UserInputType ==
		Enum.UserInputType.Touch then

		miniDragging = true

		miniDragStart =
			input.Position

		miniStartPos =
			miniFrame.Position

		input.Changed:Connect(function()

			if input.UserInputState ==
				Enum.UserInputState.End then

				miniDragging = false
			end
		end)
	end
end)

UserInputService.InputChanged:Connect(function(input)

	if miniDragging and (
		input.UserInputType ==
		Enum.UserInputType.MouseMovement
		or input.UserInputType ==
		Enum.UserInputType.Touch
	) then

		local delta =
			input.Position - miniDragStart

		miniFrame.Position =
			UDim2.new(
				miniStartPos.X.Scale,
				miniStartPos.X.Offset + delta.X,

				miniStartPos.Y.Scale,
				miniStartPos.Y.Offset + delta.Y
			)
	end
end)

--==================================================
-- TARGET SEARCH
--==================================================

local function getTargets()

	local targets = {}

	for _,object in ipairs(
		workspace:GetDescendants()
	) do

		if (
			object:IsA("BasePart")
			or object:IsA("Model")
		)
		and table.find(
			targetNames,
			object.Name
		) then

			table.insert(
				targets,
				object
			)
		end
	end

	return targets
end

--==================================================
-- TELEPORT WITHOUT TURNING
--==================================================

local function teleportWithoutTurning(
	character,
	target
)

	local camera =
		workspace.CurrentCamera

	local humanoid =
		character:FindFirstChildOfClass(
			"Humanoid"
		)

	if not humanoid then
		return
	end

	local oldCharacterCFrame =
		character:GetPivot()

	local oldCameraCFrame =
		camera.CFrame

	local oldCameraType =
		camera.CameraType

	local oldAutoRotate =
		humanoid.AutoRotate

	humanoid.AutoRotate = false

	local targetPosition =
		target:GetPivot().Position

	local rotationOnly =
		oldCharacterCFrame
		- oldCharacterCFrame.Position

	character:PivotTo(
		CFrame.new(
			targetPosition
			+ Vector3.new(0,4,0)
		)
		* rotationOnly
	)

	camera.CameraType =
		Enum.CameraType.Scriptable

	camera.CFrame =
		oldCameraCFrame

	local connection =
		RunService.RenderStepped:Connect(
		function()

			if camera and camera.Parent then
				camera.CFrame =
					oldCameraCFrame
			end
		end
	)

	task.wait(0.7)

	connection:Disconnect()

	if character.Parent then

		character:PivotTo(
			oldCharacterCFrame
		)

		camera.CFrame =
			oldCameraCFrame

		camera.CameraType =
			oldCameraType

		humanoid.AutoRotate =
			oldAutoRotate
	end
end

--==================================================
-- FARM LOOP
--==================================================

task.spawn(function()

	while true do

		if farmEnabled then

			local character =
				player.Character

			if character then

				local targets =
					getTargets()

				for _,target in ipairs(
					targets
				) do

					if not farmEnabled then
						break
					end

					if target.Parent then

						teleportWithoutTurning(
							character,
							target
						)

						task.wait(0.7)
					end
				end
			end
		end

		task.wait(0.2)
	end
end)

--==================================================
-- RESPAWN
--==================================================

player.CharacterAdded:Connect(function()

	if ghostLoop then
		ghostLoop:Disconnect()
		ghostLoop = nil
	end

	if flyLoop then
		flyLoop:Disconnect()
		flyLoop = nil
	end

	if bodyClone then
		bodyClone:Destroy()
		bodyClone = nil
	end

	task.wait(1)

	if ghostFlyEnabled then

		startGhostFly()

	elseif normalFlyEnabled then

		startNormalFly()
	end
end)
